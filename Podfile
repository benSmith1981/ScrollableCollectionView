# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ScrollableCollectionView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ScrollableCollectionView
pod 'Kingfisher'
  target 'ScrollableCollectionViewTests' do
    inherit! :search_paths
    pod 'Kingfisher'
    # Pods for testing
  end

  target 'ScrollableCollectionViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
